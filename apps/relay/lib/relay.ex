defmodule Relay do
  @moduledoc """
  R2 Transport Relay — routes R2-WIRE frames between devices in a trust group.

  The relay is untrusted and stateless. It forwards opaque encrypted frames
  without reading, modifying, or storing them. R2-TRUST provides end-to-end
  security between devices.
  """
end
