defmodule Notekeeper do
  @moduledoc """
  R2 Notekeeper — distributed notetaking capability for R2 trust groups.

  A swarm of sentants that syncs Markdown notes across all devices in a
  trust group without Tailscale, without central servers, without
  third-party accounts.
  """
end
